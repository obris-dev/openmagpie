// Public surface of the generated contract. `generated.ts` holds the full
// contract (every wire shape); this file re-exports the pieces the web app
// consumes today. Add exports here as screens start using more of the contract.

export { AuthUserSchema } from "./generated";
export type { AuthUser } from "./generated";
