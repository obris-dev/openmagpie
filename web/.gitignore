node_modules
.next
dist
.turbo
*.log
.DS_Store
.env*.local
# OpenNext / wrangler (Cloudflare Workers) build output + local secrets
.open-next
.wrangler
.dev.vars
cloudflare-env.d.ts
# Generated at build by the marketing `copy-installer` script (the canonical
# source is scripts/quickstart/bootstrap.sh); served as the curl|sh installer.
apps/marketing/public/install.sh
# Auto-generated by every `next dev` / `next build` (its route-types import
# points at .next/dev/types vs .next/types, so a committed copy churns).
# Regenerated by any next command, so it's safe to ignore.
next-env.d.ts
